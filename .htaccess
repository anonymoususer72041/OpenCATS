Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Do nothing if the request is already for /public or /public/...
  RewriteRule ^public(/|$) - [L]

  # Otherwise, forward everything to /public/...
  RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
